# 验证记录

## 已完成

- Linux 环境安装 .NET SDK 10.0.401，Release 构建通过：0 警告、0 错误。
- 已更新 SQLite 原生依赖，消除构建时报告的 NU1903 依赖漏洞警告。
- 真实 HTTP 服务 + 临时 SQLite 的集成测试通过：
  - 首页静态资源可访问。
  - 新建 Item、数字属性、Item 引用。
  - 禁止删除被引用对象。
  - 拒绝过期版本写入、无效引用和错误属性类型。
  - 拒绝标签继承循环。
  - 文件导入生成暂存对象；未确认前不能预览或被正式对象引用。
  - 重复路径跳过；编辑建议后确认入库。
  - JSON 导出与进程重启后数据持久化。
  - 拒绝跨来源调用、无效 Host 和无客户端标记的写请求。
  - 删除记录不删除原文件。
- JavaScript 通过 Node.js 语法检查。
- Windows x64 自带运行时发布成功，包含前端静态资源。

## 未验证

- 初版 GitHub Actions 已通过（run 35459588465）；本次修复的 Actions 结果以对应提交为准。
- 本环境为 Linux，Windows 启动器、资源管理器定位及 Windows 原生运行未实机测试。
- 内置云端浏览器已连接，但访问测试服务 http://127.0.0.1:5278 返回 ERR_BLOCKED_BY_CLIENT。未完成真实浏览器点击、布局、媒体解码检查；DOM 测试不能替代这些验证。

发布包属于可试用初版；请使用少量测试收藏验证，再迁入大量资料。

## 2026-09-20 云端调试

- 在与 main 初版相同的 9bf74d4 源码上，.NET SDK 10.0.401 Release 构建通过，0 警告、0 错误；原 HTTP + SQLite 集成测试通过。
- 新增 tests/ui.mjs：jsdom 加载真实 HTML/JS，通过真实 HTTP 接口连接临时 SQLite 测试服务；不使用正式数据。
- 在修复前复现三项失败，修复后全部通过：清空推荐属性不再存回空值；导入清除旧搜索条件；从暂存区手动新建后转入正式收藏。
- 同时通过演员/电影创建、引用和反向跳转、暂存修改后确认入库的 DOM 与 HTTP 检查，无未处理 DOM 错误。
- UI 回归测试接入 GitHub Actions；Node.js 与 jsdom 只用于测试，不增加程序运行依赖。

## 手机浏览器版

- 新增手机导航、独立详情/返回、上传暂存、局域网口令登录、会话退出失效。
- Release 编译及原后端回归、DOM 交互回归在云端验证。
- DOM 测试额外覆盖菜单切换、未保存返回保护，以及局域网 HTTP 中 randomUUID 不可用的兼容路径。
- tests/mobile.py 使用真实私有网卡验证局域网鉴权、上传暂存、失败上传清理、媒体保护及退出会话；接入 Windows Actions。
- 当前云端只有回环网卡，无法在此启动局域网实例；LAN 测试结果以 Windows Actions 为准。
- 尚未进行 Android/iPhone 实机及真实浏览器视觉验证；防火墙与 Wi-Fi 隔离需在使用环境确认。
